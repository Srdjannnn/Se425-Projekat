# Club Ticket Selling System

A simple demo web application for selling tickets to club events using Python Flask.

## Features

- View upcoming events
- Select available seats for events
- "Purchase" tickets (demo only, no real payment)

## Setup

1. Ensure Python 3.12 is installed.
2. Install dependencies: `pip install -r requirements.txt`
3. Run the app: `python app.py`
4. Open http://127.0.0.1:5000/ in your browser.

## Project Structure

- `app.py`: Main Flask application
- `templates/`: HTML templates
- `static/`: CSS and JavaScript files
- `docs/`: Documentation
- `requirements.txt`: Python dependencies

## Git Repository

Projekat koristi Git za verzionisanje. Repozitorijum je inicijalizovan lokalno. Da biste ga objavili na GitHub-u:

1. Kreirajte novi repozitorijum na GitHub-u.
2. Dodajte remote: `git remote add origin <URL>`
3. Push: `git push -u origin master`

## Trello Board

Za organizaciju projekta koristite Trello board. Kreirajte board sa listama za faze projekta (P1 N1, P1 N2, itd.) i karticama za zadatke. Pošaljite link asistentu i profesoru.